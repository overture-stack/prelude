Adopted the agentics development context at the Prelude project level; universal conventions come from global context, so project files are kept lean.

- `AGENTS.md`: canonical agent-neutral Prelude conventions — version/sync marker (agentics 0.2.0), project context, project-specific constraints, repo orientation; dispatches universal conventions to the agentics clone rather than copying them
- `CLAUDE.md`: stub pointing at `AGENTS.md`; notes universal conventions live in global context
- `.dev/roadmap.md`: seeded from repo state — config directory reorganization marked `[in progress]`, fixture tables open; full aims left to README
- `.dev/tech-debt.md`: format header plus one entry (`requirments.md` misspelling)
- Skipped by design: `conventions/`, `CLAUDE.roles/`, `CLAUDE.softeng.md`, and the per-project credential hook — all already provided by the developer's agentics-based global context (`~/.claude/`)

Populated `.dev/roadmap.md` from the `requirments.md` scoping doc so the roadmap reflects the two grant Aims rather than a repo-state guess.

- `.dev/roadmap.md`: replaced the initial seed with the real near-term tracker — grouped by readiness (start-now / needs-arranger-input / parallel app work / gated-on-ARGO / reporting), each item a distilled `§`-ref pointer into `requirments.md` rather than a copy, keeping that doc the single source of detail

Session-start sync reconciled the `.dev/` docs against commit `b51998e`: the config reorg is now committed and complete, and the developer removed the root scoping doc, so its pointers no longer resolve.

- `.dev/roadmap.md`: removed the finished config-directory-reorganization item; stripped the dangling `requirments.md` link and its `§`/statement pointers so each item stands alone
- `.dev/tech-debt.md`: dropped the obsolete `requirments.md` rename entry (file no longer exists); logged the dead `configs/nginxConfigs/` ignore line the reorg left in `.gitignore`

Migrated the demo stack from Elasticsearch to OpenSearch in docker-compose so search runs on the Apache-2.0 engine, and fixed a compose regression that was blocking startup.

- `configs/opensearch/`: renamed from `configs/elasticsearch/` (git mv); the mappings are plain ES7 index templates (keyword/date/object/boolean) and load into OpenSearch unchanged
- `docker-compose.yml`: replaced the `elasticsearch` service with `opensearch` (image `opensearchproject/opensearch:${OPENSEARCH_TAG:-2.17.1}`, security plugin disabled so the localhost demo stays on plain HTTP, `compatibility.override_main_response_version` on); repointed the setup `ES_URL`/`ES_INDEX_CONFIG_DIR`, conductor `--es-host`, arranger `ES_HOST`, and the data volume to opensearch
- `docker-compose.yml`: moved the `&default-logging` anchor back above `services:`; commit `b51998e` had relocated it to the file end, which breaks `docker compose` (alias referenced before its anchor) and blocked `make demo` independently of the migration
- Runtime compatibility (conductor/arranger ES client product check against OpenSearch) is not yet verified: logged in tech-debt with the fallback fix

Diagnosed the `make demo` failures against the running stack; the OpenSearch migration is functionally verified and both failures are pre-existing.

- OpenSearch healthy, reports 7.10.2 (compat override), all 6 indices created; Arranger reads the real mappings, so the ES-client product check did not fire
- Arranger crash-loops because `configs/arranger/argo_clinical` points at `esIndex: donor_centric`, which is never created (ARGO pending): pre-existing, would 404 on Elasticsearch too
- conductor-cli exits 1 on CSV data validation (`correlation.csv` vs table schema), leaving indices empty: pre-existing, unrelated to the search engine
- `.dev/tech-debt.md`: retired the product-check risk as resolved; logged the two pre-existing `make demo` blockers

Resolved the Arranger startup failure; the OpenSearch search + MCP layer (opensearch, arranger, arranger-mcp) now runs healthy and serves catalogue GraphQL.

- Root cause was the migration's own `compatibility.override_main_response_version`: it hides `version.distribution` on `GET /`, so Arranger auto-detected the Elasticsearch client (which then fails against OpenSearch) instead of its native OpenSearch client. Correcting the earlier note in this file: the deeper blocker was this client mis-selection, not the missing ARGO index alone
- `docker-compose.yml`: removed the override and left a NOTE warning against re-adding it; `GET /` now reports `distribution: opensearch`, number `2.17.1`
- `configs/arranger-pending/argo_clinical`: relocated the premature ARGO catalogue out of the arranger config mount (its `donor_centric` index is not created until ARGO data lands); added `configs/arranger-pending/README.md` with re-enable steps
- Verified: arranger + arranger-mcp healthy, `POST /correlation/graphql {__typename}` returns `Root`
- Still open (pre-existing, unrelated to the engine): conductor CSV data validation leaves indices at 0 docs; stage container is unhealthy

Diagnosed the conductor 0-docs failure to root cause: it is the `@elastic/elasticsearch` 7.17 product check rejecting OpenSearch, not a data problem.

- Isolated it: PostgreSQL loads all 999 rows and a direct `_bulk` curl of conductor-style docs indexes fine; only conductor's ES-client bulk fails. The `--debug` run shows "The client noticed that the server is not Elasticsearch and we do not support this unknown product"
- Confirmed the CSV/mapping are fine: no ragged rows, no empty/non-numeric cells in numeric columns, and a well-formed doc indexes as `created`
- Corrected the tech-debt entry (was mislabeled a CSV/schema mismatch); the real fix is making conductor use the OpenSearch client (or a header-injecting proxy interim)
- Note: conductor wraps the product-check error as "Data validation failed during upload" and hides it without `--debug`, a conductor UX bug worth flagging upstream

Fixed conductor for OpenSearch by switching it to the OpenSearch client; the full demo pipeline now loads and serves data.

- `apps/conductor/package.json` + `package-lock.json`: replaced `@elastic/elasticsearch@^7.17` with `@opensearch-project/opensearch@^2.13`; the OpenSearch client is a fork of the ES 7.10 client so the API (`bulk`, `indices.*`, `cat.*`, `info`, `ping`, `{body}` responses) is drop-in and `tsc --noEmit` passes with only the import path changed across 7 files
- `docker-compose.yml`: conductor-cli now `build:`s from `apps/conductor/Dockerfile` (image `conductor-opensearch:local`) instead of pulling the ES-client published image; dropped the amd64 platform pin so it builds native
- Verified end to end: all five tables load (`999/999/999/999/44`), OpenSearch index counts match, and Arranger returns those hit totals via `{ records { hits { total } } }`
- Follow-ups logged: upstream the conductor client change and switch back to a published tag; stage still unhealthy; residual ES naming is cosmetic

Closed out the three follow-ups; the whole stack is now healthy.

- Stage: root-caused `unhealthy` to the healthcheck's `wget http://localhost:3000/` resolving to IPv6 `::1` while Stage listens on IPv4 (`connection refused`); same cause as the next-auth SSR `CLIENT_FETCH_ERROR`. `docker-compose.yml`: stage healthcheck `localhost` to `127.0.0.1`, container now healthy; the portal already served 200 from the host
- `.gitignore`: deduped the repeated `docs/developer/05-Regression-Testing.md`; `configs/nginxConfigs/` was already removed externally. `arranger/` was also removed from `.gitignore` externally: left as-is (plausibly deliberate prep for the arranger submodule work), flagged for confirmation
- Conductor upstream: there is no separate repo to PR: `overture-stack/conductor` redirects to `overture-stack/prelude` and conductor's source is `apps/conductor` in this repo, so the change just needs committing here plus a CI image republish
- Residual ES naming: recommended keeping. The `ES_*` env vars are a required interface Arranger and conductor read, so a full rename is impossible and the rest is zero-value churn
