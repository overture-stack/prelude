Migrated the config-side ARGO + Drug Discovery artifacts from the untracked `overture-configs/` reference snapshot into Prelude's type-grouped `configs/`, resolving a mapping-format and index-naming mismatch so the ARGO catalogue can load through the existing template-based loader.

- `configs/opensearch/donor-mapping.json` (was `clinical-mapping.json`): wrapped the bare `{settings,mappings}` create-index body as a legacy index template (`index_patterns: ["donor-*"]`, alias `donor_centric`) so the `_template`-based loader actually applies the mapping; renamed to the `donor` convention (→ index `donor-index`, alias `donor_centric`). The reference and prior copy were a bare body that would have loaded a pattern-less template matching nothing
- `configs/lectern/{correlation,expression,mutation,protein}.json`: added the four Drug Discovery Lectern schemas (previously absent); the ARGO dictionary already present at v142.3 was kept (reference snapshot was older v142.1)
- `configs/arranger-pending/argo_clinical/base.json`: `esIndex` `donor_centric` → `donor-index`, matching how Drug Discovery catalogues point at the concrete `<name>-index`; catalogue stays parked pending data load
- `configs/arranger-pending/README.md`: updated — a `donor-mapping.json` now exists so setup creates an empty `donor-index`; the missing-index 404 that kept the catalogue parked is resolved, remaining blockers are the data load + Stage/MCP wiring
- `.dev/roadmap.md`: ARGO-wiring item now records the config migration done + remaining runtime wiring; Lectern-UI item notes the dictionaries now exist

Not done (deliberately out of scope, flagged as the runtime "ARGO wiring" follow-up): promoting `arranger-pending → arranger`, the conductor `DATA_TABLES` loop entry, the Stage datatable slot, MCP catalogues, healthcheck list, and a `make demo` verification. The `overture-configs/` reference clone remains untracked and needs a keep/remove/ignore decision; its `STRUCTURE.md` is stale (describes an `argo-file-centric`/`file_centric` layout that does not match its actual `argo-data`/`clinical` contents).

Corrected the AGENTS.md repository-orientation entry after the OpenSearch migration and config additions (developer-authorized instruction-file edit).

- `AGENTS.md`: `configs/` bullet now lists `opensearch/` (was `elasticsearch/`) and `lectern/`, and notes the ARGO `donor` catalogue parked in `arranger-pending/`

Built the ARGO join ETL and loaded the clinical data into OpenSearch as nested donor-centric documents, because conductor's flat-CSV upload cannot express the 15-entity join. The developer removed the `overture-configs/` reference clone, so that earlier open decision is resolved.

- `data/tables/argo_clinical/build_donor_documents.py`: joins the 15 normalized ARGO TSVs into nested donor documents (donor → 8 direct child entities + `treatment` → 5 therapy children); field set and per-field type coercion are read from `donor-mapping.json` so the output cannot drift from the index it targets; emits `_bulk` NDJSON and reports counts/orphans/coercion. Mirrors `data/fixtures/build_fixtures.py`
- `data/tables/argo_clinical/donor.ndjson`: generated artifact (373 donors, ~2.4 MB) loaded into `donor-index` via `_bulk` with zero errors; verified nested and nested-in-nested (`treatment.chemotherapy`) queries plus a `treatment_type` aggregation. Load was a direct insert into the running index — not yet part of `make demo` (logged in tech-debt)
- Dropped the stale pre-rename `clinical-index` (empty) and its template from the running cluster; `donor-index` now created the setup way (template → empty index), which also validated the `donor-mapping.json` template wrapper end to end
- `.dev/roadmap.md`, `.dev/tech-debt.md`: ARGO-wiring item now records the ETL + data load done and the remaining promotion/Stage/make-demo work; tech-debt logs the non-reproducible load

Trimmed `data/tables/argo_clinical/` to demo-minimal (developer chose artifact-only): kept the loadable `donor.ndjson`, removed the 15 source TSVs and `build_donor_documents.py`.

- `data/tables/argo_clinical/`: now holds only `donor.ndjson` (373 nested donor docs), which becomes the canonical committed source. Trade accepted: no in-repo regeneration path remains — the join logic and raw ARGO extract are no longer in the repo, so structural changes require rebuilding from a fresh extract
- `.dev/roadmap.md`, `.dev/tech-debt.md`: reconciled to the artifact-only state — the pending `make demo` load step is now a plain `_bulk` of `donor.ndjson`, not an ETL regeneration

Completed the runtime ARGO wiring: the donor-centric clinical catalogue is now served end to end (Arranger + MCP + Stage) and loads reproducibly on a clean `make demo`. Verified against a full teardown + rebuild.

- `configs/arranger/donor/`: promoted from `arranger-pending/argo_clinical`; fixed `documentType` `donor` → `records` — the reference value diverged from every other catalogue and would have broken the Stage query and the `{ records { … } }` pattern. Catalogue endpoint is the directory name, so it serves at `/donor`
- Renamed 4 GraphQL-illegal hyphenated field names to underscores (`ca19-9_level`, `pd-l1_status`, `pan-trk_ihc_status`, `non-hematological_toxicity`) across `donor-mapping.json`, the donor `extended/table/facets.json`, and `donor.ndjson`: Arranger 500'd building the donor GraphQL schema until this was fixed
- `docker-compose.yml`: added `donor` to the `arranger` healthcheck list and the MCP `ARRANGER_CATALOGUES` default; reassigned Stage datatable slot 5 from fixture to donor (`/donor`, `donor_centric`, export id `submitter_donor_id`); mounted `./data` into the setup container
- Stage source: renamed the slot-5 page `pages/fixtureTable → pages/donorTable`, retitled to “Clinical Data”, updated the `FIXTURE_TABLE → DONOR_TABLE` route enum, the NavBar link, and the Explore-Data dropdown exclusion. fixture stays a loaded, searchable Arranger/MCP catalogue — only its Stage tab was reassigned
- `setup/scripts/services/elasticsearch/load_donor_documents.sh` (new) + `platform.sh`: after `setup_indices.sh` creates `donor-index`, bulk-loads `donor.ndjson` from the setup container (which has curl; conductor's flat-CSV loader can't ingest the nested file). No-ops if the artifact is absent
- `.env.example`, `configs/arranger-pending/README.md`: added `donor` to the example MCP catalogues; rewrote the pending README as a general staging-mechanism doc (nothing currently pending)
- Verified by `docker compose down -v` + full demo up: setup loaded `donor-index` (373 root docs; `_cat` reports 4919 counting nested children), all 6 Arranger catalogues healthy (`/donor` → 373), MCP validated `donor`, Stage `/donorTable` → 200 and `/fixtureTable` → 404
- `.dev/roadmap.md`, `.dev/tech-debt.md`: removed the completed ARGO-wiring item; resolved the non-reproducible-load entry; logged the 245-column donor table as low-priority polish

Surfaced the clinical data table in the auto-discovered "Explore Data" navbar dropdown and the homepage "Explore the Data" card, alongside correlation/expression/mutation/protein (previously excluded, matching the same exclusion fixture had before the slot-5 rename).

- `apps/stage/global/utils/dataTablesDiscovery.ts`: removed the `donorTable` exclusion; both surfaces read the same `/api/data-tables` discovery, so one change covers both
- `apps/stage/global/utils/tableConfig.ts`: registered `donorTable` → "Clinical Data" so it matches the dedicated NavBar tab's label instead of auto-generating "Donor Table"
- The dedicated NavBar "Clinical Data" tab is unchanged and still works — the table is now reachable both ways

Restored the Lectern dictionary viewer (removed wholesale at this repo's `initial commit`) as a navbar-linkable "Dictionary" dropdown, replacing the dedicated Clinical Data nav slot with a dropdown covering all 5 dictionaries, per the roadmap's "re-enable the Lectern dictionary UI" item.

- `apps/stage/public/dictionary`: replaced 2 unused placeholder JSONs with a symlink to `configs/lectern/`, mirroring the existing `public/docs → docs/` pattern; `Dockerfile` swaps it for a real copy at build time (mirrors the docs COPY step) — single source of truth, no duplicated dictionary files
- `apps/stage/components/pages/dictionary/DictionaryViewer.tsx` (restored, simplified): renders one dictionary via `@overture-stack/lectern-ui`'s `HostedDictionaryDataProvider` + `DictionaryHeader` + `DictionaryTableViewer`, all wrapped in one `DictionaryTableStateProvider` (both Header and TableViewer read from its context — the initial cut under-scoped this and 500'd with "useDictionaryStateContext must be used within a DictionaryStateProvider" until fixed). No custom fetch hook needed: the installed 1.0.0 package's `HostedDictionaryDataProvider` already fetches/validates/handles loading+error state, superseding the old (pre-removal) hand-rolled `useDictionary` hook
- `apps/stage/pages/api/dictionaries.ts` (new): fs-discovers `public/dictionary/*.json`, titles each from its own Lectern `name` field (snake_case → Title Case; names that already read as prose, e.g. the ARGO dictionary's "ICGC-ARGO Data Dictionary", pass through unchanged)
- `apps/stage/pages/dictionary/[name].tsx` (new): dynamic route, `DictionaryViewer` fetches `/dictionary/${name}.json` directly (static asset, no API round-trip for content)
- `apps/stage/components/NavBar/DictionaryDropdown.tsx` (new, mirrors `DataTablesDropdown.tsx`) replaces the dedicated `/donorTable` link in `NavBar.tsx`; the now-dead `INTERNAL_PATHS.DONOR_TABLE` enum member was removed (donorTable stays reachable via the unrelated Explore Data dropdown/homepage, which use dynamic paths, not this enum)
- Decision: static-JSON dictionaries, not a deployed Lectern server — no new service, and the symlink+bind-mount-at-source approach already gives env-configurability across dev environments
- Not restored: `DictionaryPlayground.tsx` (a 1757-line editor, unrelated to linking dictionaries for viewing) and `useDictionaryHydration.ts` stays a no-op (it embedded dictionary tables inside documentation markdown prose — a distinct feature from the navbar dropdown, out of scope for this ask)
- Verified live: `/api/dictionaries` lists all 5 with correct titles, each `/dictionary/<id>` returns 200 and its `.json` asset is byte-identical to `configs/lectern/`, the dedicated Clinical Data nav link is gone from the NavBar bundle, and `/api/data-tables` (Explore Data dropdown + homepage) is unaffected

Reviewed the dictionary viewer for bugs and feature completeness against the actual `overture-stack/lectern` upstream source (cloned to compare, since the locally installed npm build's `.d.ts` under-documents its own `.js`), fixing a real duplicate-header bug and wiring the meta-driven features the ARGO dictionary was authored for.

- `apps/stage/components/pages/dictionary/DictionaryViewer.tsx`: fixed a duplicate-header bug — `DictionaryTableViewer` renders its own `DictionaryHeader` internally (confirmed by reading the installed package's compiled `.js`, not just its `.d.ts`, which is why it wasn't obvious from types alone); the previous pass also rendered one manually, producing two. Removed the manual one; added a `filterDropdowns` passthrough prop
- `apps/stage/global/utils/dictionaryFilterConfig.ts` (new) + `pages/dictionary/[name].tsx`: wired ARGO's `meta.category` as a "Clinical Domain" filter dropdown, scoped to that dictionary only — checked all 4 Drug Discovery dictionaries first and confirmed they carry no schema/field `meta` beyond `displayName` (already rendered natively), so a generic filter would've been empty noise for them
- No extra wiring needed for the entity-relationship diagram: `DictionaryTableViewer` computes it automatically from the dictionary's own `foreignKey`/`uniqueKey` restrictions (`buildRelationshipMap`), gated behind a "Diagram View" toolbar button — confirmed via a real headless-browser check (Playwright, not curl) that it renders 15 connected nodes for ARGO and 1 for correlation, matching each dictionary's actual schema count, with zero console errors
- Confirmed the download-template toolbar button is correctly *absent* in static-JSON mode: its source shows it returns `null` without a live `lecternUrl` in context — genuinely a server-only feature, not a bug, and exactly the kind of feature the task said to exclude
- Investigated `customColumns` (the feature `meta.tier`/`meta.cadsr_cde` need): confirmed via `npm view` that published `1.0.0` is npm's actual latest, but its compiled `.js` predates `customColumns` entirely — the feature exists only in unreleased upstream source (one commit ahead, PR #447), which also requires React 19 while Stage is pinned to React 17. Did not attempt a git-pinned install: the package depends on sibling `workspace:*` packages not independently buildable, and forcing a two-major React bump is a much larger, separate change. Logged as tech-debt, not applied
- Verification this time used a real headless browser (Playwright, installed to the scratchpad, not the repo) against the live containers, not just HTTP status codes — screenshotted the ARGO diagram view and confirmed all 5 "Clinical Domain" category values render in the filter

Consolidated the 4 Drug Discovery Lectern dictionaries (correlation/expression/mutation/protein) into one `drug-discovery.json` with a schema entity per data table, mirroring how the ARGO dictionary already consolidates its 15 entities into one file.

- `configs/lectern/drug-discovery.json` (new, replaces `correlation.json`/`expression.json`/`mutation.json`/`protein.json`): pure concatenation of the 4 schemas (verified field counts unchanged: 17/8/14/21) under one dictionary — `name: "Drug Discovery Dictionary"`, `version: "1.0"` (all 4 already agreed), one combined description. Named the `name` field as prose (not snake_case) because `DictionaryHeader` renders it verbatim with no prettification — the dropdown-only title heuristic in `pages/api/dictionaries.ts` doesn't reach the in-page header, a real (if minor) inconsistency worth fixing at the data source
- The 4 correlation/expression/mutation/protein Arranger, OpenSearch, and Postgres configs are untouched — this is a Lectern-layer-only consolidation, unrelated to the search/index/db configs per table
- No Stage code changes needed beyond one stale doc-comment path: `/api/dictionaries` already auto-discovers `public/dictionary/*.json` by filename, so it now lists 2 dictionaries (ARGO, Drug Discovery) instead of 5, with no code change
- Verified live: `/dictionary/drug-discovery` renders one header/toolbar with all 4 schemas as accordion sections, ERD button shows 4 disconnected nodes (correct — the 4 tables have no cross-references), zero console errors; the old individual routes (e.g. `/dictionary/correlation`) degrade gracefully to the library's own error UI rather than crashing, since Next's dynamic route still 200s at the page-shell level while the underlying JSON fetch 404s

Renamed the NavBar "Dictionary" label to "Data Dictionaries", and grouped the Explore Data dropdown + homepage "Explore the Data" card by dataset (ARGO Clinical vs Drug Discovery) so the two programs read as distinct sets instead of one flat list of 5.

- `global/utils/tableConfig.ts`: added an optional `group` field to `TableConfig` (donorTable → "ARGO Clinical"; the 4 drug-discovery tables → "Drug Discovery") and exported `DATA_TABLE_GROUP_ORDER` as the single shared display-order source, so the NavBar dropdown and homepage card can't drift into showing groups in a different order from each other
- `global/utils/dataTablesDiscovery.ts`: passes `group` through to `/api/data-tables`
- `components/NavBar/DataTablesDropdown.tsx`, `components/pages/home/HomeNavigation.tsx`: group items under a section-header row per dataset, mirroring the exact pattern `DocumentationDropdown.tsx` and the homepage's own "Project Documentation" card already use (`data-no-hover` + `stopPropagation` on the header so clicking it can't be mistaken for closing/navigating; `isHeader` items in the homepage's existing generic subItem renderer, unchanged)
- `components/NavBar/DictionaryDropdown.tsx`: "Dictionary" → "Data Dictionaries" (both the dropdown label and the single-item fallback text)
- Verified live in a real browser: both surfaces show "ARGO CLINICAL" / "DRUG DISCOVERY" section headers (uppercase via CSS, not a rendering bug — Playwright's `innerText()` reflects `text-transform`, which is why an early case-sensitive check false-negatived before the screenshot caught it) with the right items nested under each; clicking a real item (not a header) still navigates correctly
- Note: the NavBar label was subsequently shortened to "Dictionaries" (by the developer/linter, not this change)

Removed the "Diagram View" toolbar button from the dictionary viewer, since `@overture-stack/lectern-ui`'s `Toolbar`/`DictionaryTableViewer` renders it unconditionally with no prop to omit it.

- `components/pages/dictionary/DictionaryViewer.tsx`: hides any `<button>` whose text is exactly "Diagram View" via a `MutationObserver` scoped to the viewer's own container ref — a one-shot effect isn't enough since the toolbar mounts asynchronously once `HostedDictionaryDataProvider`'s fetch resolves, after the effect's initial run. Targeting by the button's stable label text (confirmed via live DOM inspection: `textContent` is exactly `"Diagram View"`, no extra whitespace/icon text) since there is no class, test-id, or prop distinguishing it from the toolbar's other buttons
- Verified live: the button is present in the DOM but has zero visible instances (`display: none` applied); Table of Contents, Collapse All, and By Required remain visible and unaffected; no layout gap or console errors
- **Found but not removed, flagging for a decision:** the ARGO dictionary's field tables also show per-field "View in diagram" links (e.g. next to foreignKey fields) that open the *same* diagram modal through a separate entry point the task didn't name. The toolbar button is gone; the modal itself is still reachable through these. Left alone since the request named one specific button — say if these should go too
