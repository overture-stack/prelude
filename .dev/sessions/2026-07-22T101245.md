Reworked the donor Arranger catalogue's display names, facets, and default columns so the clinical table reads naturally and defaults to fields that actually carry data, measured against the 373 loaded donors; browser-verified against the running stack.

- `configs/arranger/donor/extended.json`: rewrote 225 nested-field display names from snake_case entity prefixes to natural English (`sample_registration:` becomes `Sample Registration:`, `follow_up:` becomes `Follow-up:`), and fixed real errors: the "Hemotological"/"Recurrance" typos, a wrong "immunotherapy: Chemotherapy Drug Name" copy-paste label, a trailing space, and drug labels that repeated the entity already in the prefix
- `configs/arranger/donor/facets.json`: trimmed the facet panel from 122 active / 31 shown to a curated 27 grouped across donor demographics, diagnosis, specimen/pathology, treatment, follow-up, biomarker, and lifestyle; all are populated keyword fields plus three numeric range facets (age at diagnosis, survival time, BMI, which render as sliders)
- `configs/arranger/donor/table.json`: default columns cut from 20 (many empty, e.g. `country_of_residence_at_enrollment` at 0%) to 8 root-level fields all >=50% populated (most 92-100%): donor/program IDs, primary site, vital status, survival time, cause of death, genetic disorders, BMI
- Constraint found and worked around: nested-object fields cannot be table columns on this Arranger build (query-depth-7 cap 400s the data fetch at depth 8), so clinical dimensions like cancer type and stage are surfaced as facets rather than columns; logged in tech-debt with two possible fixes
- `.dev/tech-debt.md`: replaced the "245 columns unwieldy" entry with the current state (default view now curated) plus the nested-column depth-limit constraint
- Verified live in a real browser (Playwright against the running stack): donor table shows all 8 columns populated over 373 records with zero console/network errors, and the 27 facets render with correct counts and working numeric sliders
